name: check

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  drift:
    name: documentation matches the site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 check.py -v
